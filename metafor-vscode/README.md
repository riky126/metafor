# Metafor PTML Support

Provides syntax highlighting for Metafor components in VS Code.

## Features

- Syntax highlighting for `.ptml` files
- Support for Embedded Python blocks
- Support for Metafor directives like `@component`, `@props`, `@value`
- HTML highlighting within `@ptml` blocks
